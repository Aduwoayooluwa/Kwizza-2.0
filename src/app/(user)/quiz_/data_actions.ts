"use server"

interface UserDetailsProps {
    first_name: string;
    last_name: string;
}

export function startQuiz(userDetails: UserDetailsProps) {
    try {
        // form function 
    }
    catch (error) {

    }
}